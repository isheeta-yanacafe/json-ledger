{
  "name": "json-ledger",
  "private": true,
  "description": "Dev/test tooling only. The app itself (json-ledger.html / json-ledger.en.html) has no build step — download it and open it in a browser.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
